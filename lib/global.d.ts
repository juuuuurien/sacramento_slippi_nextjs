export type HeaderData = {
  title: string;
  style?: string;
};

export type PlayerData = {
  id: string;
  player_tag: string;
  connect_code: string;
  approved: boolean;
  createdAt: string;
  updatedAt: string;
};
